# Test dependencies (pinned). Installed on top of runtime deps in the Docker
# `test` stage; not present in the runtime image.
-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2
asgi-lifespan==2.1.0
