# Alembic config (Standards §14 — forward-only migrations).
# The database URL is injected from application settings in migrations/env.py;
# it is intentionally NOT stored here (no secrets in VCS, Standards §6).
[alembic]
script_location = migrations
prepend_sys_path = .
path_separator = os
